datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Users {
  id        Int      @id @default(autoincrement())
  name      String
  createdAt DateTime @default(now())
}

// TODO: Pokemon (cache pokemon data)
// TODO: log message in/out(bound)
// TODO: log image generation task
